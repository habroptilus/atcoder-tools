#include <bits/stdc++.h>
typedef long long ll;
int main(int argc, char const *argv[]) {
    ll N, A, B;
    std::cin >> N >> A >> B;
    if (N == 1) {
        std::cout << "Hello World" << '\n';
    } else {
        std::cout << A + B << '\n';
    }
    return 0;
}
