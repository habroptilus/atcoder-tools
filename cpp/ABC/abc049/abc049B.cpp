#include <bits/stdc++.h>
typedef long long ll;

int main(int argc, char const *argv[]) {
    ll H, W;
    std::cin >> H >> W;
    std::string S;
    for (size_t i = 0; i < H; i++) {
        std::cin >> S;
        std::cout << S << '\n' << S << '\n';
    }
    return 0;
}
