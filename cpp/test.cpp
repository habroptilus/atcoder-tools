#include <bits/stdc++.h>
typedef long long ll;

void hoge(int a) { a *= 2; }
void fuga(int &a) { a *= 2; }

int main(int argc, char const *argv[]) {
    if (3) {
        std::cout << 3 << '\n';
    }
    if (0) {
        std::cout << 0 << '\n';
    }
    return 0;
}
